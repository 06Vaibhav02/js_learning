// && -- and
// || -- or
// ! -- not

let isLoggedIn = true;
let isPaid = false; 
console.log( isLoggedIn && isPaid ); // false -- do not proceed

let isEmailUser = false;
let isGooglUser = true;
console.log( isEmailUser || isGooglUser ); //true -- do proceed
 